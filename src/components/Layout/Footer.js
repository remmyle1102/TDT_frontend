import React from 'react';

import { Navbar, Nav, NavItem } from 'reactstrap';

const Footer = () => {
  return (
    <Navbar>
      <Nav navbar>
        <NavItem>
          TDT Audit Database Web Application
        </NavItem>
      </Nav>
    </Navbar>
  );
};

export default Footer;
